-r requirements.txt
# Test + lint stack — installed in CI and locally via `pip install -r requirements-dev.txt`
pytest>=8.3.0
pytest-asyncio>=1.3.0
pytest-cov>=5.0.0
asgi-lifespan>=2.1.0
ruff==0.15.11
pre-commit>=3.8.0
