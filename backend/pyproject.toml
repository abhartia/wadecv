[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "session"
testpaths = ["tests"]
python_files = ["test_*.py"]
# Pre-CI the repo had test files that have drifted out of sync with the current
# model layer (e.g. `User.hashed_password` renamed to `password_hash`). Fixing
# them is out of scope for the CI-bring-up PR and is tracked as a follow-up.
# They also hit live LLM APIs which we cannot pay for in CI.
addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
    "--cov=app",
    "--cov-report=term-missing",
    "--cov-report=xml",
    "--cov-fail-under=20",
    "--ignore=tests/test_fit_flow.py",
    "--ignore=tests/test_layout_iterations.py",
]
markers = [
    "integration: tests that hit a real Postgres (skipped if no DATABASE_URL_TEST)",
]

[tool.coverage.run]
branch = true
source = ["app"]
omit = [
    "app/main.py",
    "alembic/*",
    "tests/*",
]

[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["alembic/versions"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "SIM", # flake8-simplify
    "RUF", # ruff-specific
]
ignore = [
    "E501",   # line length handled by formatter
    "B008",   # fastapi Depends() in defaults is the idiom
    "B904",   # raise-from — existing handlers don't chain; would require audit
    "RUF001", # ambiguous unicode in strings — intentional in prompt templates
    "RUF006", # fire-and-forget asyncio tasks are used deliberately in routers
    "UP031",  # %-formatting is fine where it already exists
    "N806",   # existing variable naming is acceptable
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B011"]

[tool.ruff.format]
quote-style = "double"
